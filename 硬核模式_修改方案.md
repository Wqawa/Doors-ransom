# Ransom 硬核模式 —— 修改方案 / 计划单（v0.4 · rev3 · 定稿）

> 目标工程：`C:\Users\S062PT\Desktop\Ransom`（源码停在 0.3）
> 本文只做方案，**未改任何代码**。行号对应现在的源码，改之前以文件实际内容为准。
> 不动的目录：`C:\Users\S062PT\Desktop\d`（同一工程的 git 副本）。
>
> **rev2**：Q1~Q6 全部确认；取消"限时金币"；新增 T13（安全阀按两次）。
> T9 假金币的两种效果**映射反转**且可叠加；T1 面额池改成 50/75/100；
> T11 同时锁定数改成 9 且不可重复锁同一项；T5 素材后缀是 `.ogg`。
>
> **rev3（本版）**：R1~R7 七个细节参数全部确认，**方案已无待定项**。
> 实质变化只有两条：
> 1. **假金币顶上去的赎金"会回落"** —— 真金币先拿去填假币的债，填平后才开始算进付款进度（§10.4，这是机制，不是数值）；
> 2. **锁定释放后的冷却 4 秒 → 9 秒**，**安全阀两次之间的窗口 5 秒 → 9 秒**。
> 其余（假币比例 40/40/20、开演前单次退出、磁盘扫描上限 200、关 6 窗必死）沿用默认。

---

## 0. 需求确认结果（Q1~Q6 已定）

| 编号 | 问题 | 你的决定 | 折进哪一节 |
|---|---|---|---|
| Q1 | remix 素材 | **`Ransom_theme_remix.ogg`**（你忘放进去了，后缀是 ogg） | §7 |
| Q2 | 假金币两种效果 | **前缀被污染 → 扣计时器（原面额 × 0.1）**；**后缀被污染 → 按原面额增加未付赎金**；**两者可叠加在同一个金币上** | §10 |
| Q3 | 金币生成范围 | **所有磁盘顶层目录 + 原本的桌面范围** | §11 |
| Q4 | 金币规模 | **方案 B**：面额池 50/75/100，约 74 枚 | §9 |
| Q5 | 非快捷方式锁定 | **同时最多 9 个**，按原快捷方式那样阻止交互，**同一项不允许同时锁两次** | §12 |
| Q6 | 硬核开关与退出 | **不可中途切换**（本轮跑到底）；**硬核下安全阀要按 2 次才退出** | §13 |

**本轮取消**：**限时金币**（原硬核清单里的那条）。
理由你说得很清楚：Windows 上删掉桌面文件后图标不会立刻消失，要刷新才看得到变化，做成"限时"玩家根本感觉不到。
→ 影响：T 列表里没有、也不会有这一项；金币只在被点掉/付清/超时受惩罚时才删，行为与 0.3 一致。

> 附注（只是留个底，**这次不做**）：这条限制其实有解——`SHChangeNotify(SHCNE_DELETE, SHCNF_PATHW, path, nullptr)`
> 能让 explorer 立刻把那个图标抹掉，不用等 F5。以后想恢复"限时金币"，或者想让"点掉一枚金币"当场可见，都可以用它。
> 现有 `gold::Consume()`（gold.cpp:722-741）删文件后同样有这个"图标滞后"现象，要不要顺手加个通知，你说了算。

---

## 1. 计划单总览（按建议实施顺序）

| 序号 | 任务 | 主要文件 | 难度 | 估时 | 依赖 |
|---|---|---|---|---|---|
| T1 | 硬核开关的数据层（settings） | settings.h/.cpp | ★☆☆☆☆ | 0.5h | — |
| T2 | 设置界面开关 + 抖屏 + 赎金滑块变灰 | setup_ui.cpp | ★★☆☆☆ | 2h | T1 |
| T3 | 赎金 5000 | settings.cpp | ★☆☆☆☆ | 0.25h | T1 |
| T4 | 倒计时 180 秒 | director.cpp | ★★☆☆☆ | 0.5h | T1 |
| T5 | remix 主题曲（.ogg）+ 3:30→3:00 变速不变调 | audio.cpp / audio_clip.cpp / assets | ★★★☆☆ | 2~3h | 素材 |
| T6 | 弹窗更多 + 鼠标位置弹窗 + 关窗扣 30 秒 | popup.cpp / director.cpp | ★★☆☆☆ | 1.5h | T1/T4 |
| T7 | 金币面额压到 100 以内（池 50/75/100） | settings / gold | ★☆☆☆☆ | 0.25h | T1 |
| T8 | 金币总量重算（**必改，否则无法通关**） | gold.cpp | ★★☆☆☆ | 1h | T7 |
| T9 | 假金币（名字同形替换 + 两种惩罚可叠加） | gold / director / entity_main | ★★★☆☆ | 2~2.5h | T8 |
| T10 | 金币生成到磁盘顶层目录 | gold.cpp | ★★☆☆☆ | 1.5h | — |
| T11 | 随机锁定非快捷方式 0.9~9 秒后变绿淡出（最多 9 个） | desktop_overlay.cpp | ★★★★☆ | 4~6h | T1 |
| T12 | 接线、日志、README、收尾 | director / entity_main / README | ★★☆☆☆ | 1h | 全部 |
| **T13** | **硬核下安全阀要按两次（含可见反馈）** | entity_main / setup_ui / audio | ★★★☆☆ | 1.5h | T1 |

**合计 17~22 人时**，T5（音频）和 T11（桌面逐项锁定）仍是大头，T13 代码量不大但要小心。

顺序：T1 → T3/T4 → T7/T8 → T9/T10 → T6 → T5 → T11 → **T13** → T12。每步都能单独编译、单独验证。

---

## 2. 全局设计约定

1. **硬核开关只有一个来源**：`settings::Hardcore()`。所有子系统都读它，不各自留标志位。
2. **`settings::GoldGoal()` 在硬核下返回 5000，但不改写用户存的 `goldGoal`**。关掉硬核，用户的自定义赎金原样回来。
3. **硬核常量集中在 settings.h**，不要散在各文件里。
4. **不改现有安全边界**：金币依然只写 `.lnk`、绝不覆盖已存在文件、只删登记过的路径、退出必还原。硬核只放大规模，不放松约束。
5. **判定与绘制必须同步改**（desktop_overlay 的老坑，见 §12.3）。
6. **本轮设置一旦开始不可更改**（Q6）：设置窗口一关，演出期间读到的 `settings::Hardcore()` 就不会再变——这条天然成立，**不需要**额外加锁。
7. **安全阀是红线**：两次按下这个改法必须保证"第一次一定看得见反馈"。见 T13。

---

## 3. T1 硬核开关的数据层

**`Ransom_dev\src\settings.h`**

第 42~57 行是取值范围/面额池常量区，在第 57 行（`kCoinAmountMax`）后加：

```cpp
// ---- 硬核模式 ----
// 开着硬核时的赎金目标。**不改写** g_set.goldGoal：
// 用户自己调的那个值原样留着，关掉硬核就回到它。
const int kHardcoreGoldGoal = 5000;

// 硬核的面额池：全部 <= 100（"金币面额减少"）。
// Q4 选的是方案 B —— 池子收窄到 50/75/100，让凑 5000 大约需要 74 枚。
const int kHardcoreCoinAmounts[]   = { 50, 75, 100 };
const int kHardcoreCoinAmountCount = 3;

// 硬核的勒索倒计时（3 分钟）与"关掉一个子窗口扣多少"（30 秒）。
const int kHardcoreRansomMs      = 180000;
const int kHardcoreCloseCreditMs = 30000;

// 假金币：占生成总量的百分比，以及三种污染形态的比例（前缀 / 后缀 / 两个都污染）。
// 三种加起来必须是 100。
const int kHardcoreFakePercent = 35;
const int kFakePrefixOnlyPct   = 40;   // 只污染 "Gold" -> 扣计时器
const int kFakeSuffixOnlyPct   = 40;   // 只污染面额数字 -> 增加未付赎金
const int kFakeBothPct         = 20;   // 两个都污染 -> 两种效果同时触发

// 非快捷方式随机锁定：同时最多几个、时长范围、释放后的冷却。
const int kExtraLockMax        = 9;
const int kExtraLockMinMs      = 900;
const int kExtraLockMaxMs      = 9000;
const int kExtraLockCooldownMs = 9000;   // 释放后 9 秒内不再被选中

// 硬核下安全阀"按两次"的有效窗口：两次之间超过它就要重新计数。
const int kPanicArmWindowMs    = 9000;
```

`struct Set`（第 70~97 行）在 `goldGoal`（第 87 行）后加：

```cpp
		// 硬核模式：更多弹窗 / 假金币 / 3 分钟 / 5000 赎金 / 锁非快捷方式……
		bool hardcore = false;
```

查询区（第 129~138 行）加 `bool Hardcore();`

**`Ransom_dev\src\settings.cpp`**

- `Load()` 第 213 行后加：

```cpp
            g_set.hardcore = ReadInt(path, L"game", L"hardcore", 0) != 0;
```

- `WriteIni()` 第 161 行 `L"gold_goal=%d\n"` 后插 ini 文本，并在第 166~170 行实参表按**同样顺序**插 `s.hardcore ? 1 : 0`：

```cpp
            L"; hardcore mode: 1 = 3-min timer, 5000 gold, fake coins,\n"
            L"; more popups, extra desktop locks. 0 = normal show.\n"
            L"hardcore=%d\n"
```

- 第 333 行改成：

```cpp
    // 硬核直接把赎金顶到 5000，但不动 g_set.goldGoal
    int GoldGoal() { return g_set.hardcore ? kHardcoreGoldGoal : g_set.goldGoal; }
    bool Hardcore() { return g_set.hardcore; }
```

- 第 255~259 行的启动日志带上 hardcore。

**难度 ★☆☆☆☆**。注意 `WriteIni` 的 `swprintf_s` 是位置实参，插错顺序会写出乱值。

---

## 4. T2 设置界面的硬核开关（抖屏 + 滑块变灰）

排版全是 `setup_ui.cpp` 里手算的绝对坐标（匿名 `namespace setup`，第 245 行起）。

### 4.1 腾出 60px

现在（第 248~265 行）：

```cpp
        const int kH = 470;
        const int kRow4Top = 296;       // 赎金目标金币
        const int kBtnTop  = 356;
        const int kHintTop = 412;
```

改成：

```cpp
        const int kH = 530;             // 470 -> 530，给硬核开关腾一行
        const int kRow4Top  = 296;      // 赎金目标金币（不动）
        const int kHardTop  = 356;      // 硬核模式（新增，行高同 kSafeRowH）
        const int kBtnTop   = 416;      // 356 -> 416
        const int kHintTop  = 472;      // 412 -> 472
```

窗口尺寸从 `kW/kH` 反算（`ShowSettings` 第 1163~1164 行），改了常量窗口自动变高，不用动别处。

### 4.2 `DrawCheckbox` 必须先参数化

第 451~471 行，它内部写死 `const RECT b = SafeBox();`，不改的话两个复选框会画在同一个位置：

```cpp
        // box：这个复选框方框的本地区域（由调用方给，SafeBox() / HardBox()）
        void DrawCheckbox(Graphics& g, const RectF& rc, const RECT& box,
                          bool checked, bool hot)
        {
            RectF b(rc.X + (REAL)box.left, rc.Y + (REAL)box.top, 20.0f, 20.0f);
```

第 542 行调用改成 `DrawCheckbox(g, rc, SafeBox(), g_edit.photosensitiveSafe, false);`

新增 helper（挨着第 316~330 行的 `SafeBox()` / `SafeHit()`）：

```cpp
        RECT HardBox()
        {
            RECT r;
            SetRectLocal(r, kM, kHardTop + (kSafeRowH - 20) / 2, 20, 20);
            return r;
        }

        // 可点区域和光敏那条一致：框 + 文字，不横跨整行（否则右半边没法拖窗口）
        RECT HardHit()
        {
            RECT r;
            SetRectLocal(r, kM - 4, kHardTop, 430, kSafeRowH);
            return r;
        }
```

### 4.3 画这一行 + 赎金行灰化

`PaintContent`（第 480~641 行）：

- 赎金行（第 594~626 行）加锁定态：`g_edit.hardcore` 为真时
  - 颜色用 `kTextFaint`（第 45 行）而不是 `kTextMain`/`kTextDim`；
  - 读数显示 **5000**（`settings::kHardcoreGoldGoal`），珠子画在最右端；
  - 行尾加灰字 `（硬核模式锁定）`；
  - `g_drag == 5` 的高亮不再出现。

建议在赎金块开头加：

```cpp
                const bool goldLocked = g_edit.hardcore;
                const int  shownGoal  = goldLocked ? settings::kHardcoreGoldGoal : g_edit.goldGoal;
```

- 硬核行（插在第 626 行之后、第 628 行按钮之前）：

```cpp
            // ---- 硬核模式 ----
            {
                DrawCheckbox(g, rc, HardBox(), g_edit.hardcore, false);

                const RECT hb = HardBox();
                RectF t(rc.X + (REAL)(hb.right + 10), rc.Y + (REAL)kHardTop, 260.0f, (REAL)kSafeRowH);
                DrawTextCjk(g, L"硬核模式", t, 14.0f, g_edit.hardcore ? kAccent : kTextMain,
                    StringAlignmentNear, FontStyleBold);

                RectF d(rc.X + (REAL)(hb.right + 10), rc.Y + (REAL)(kHardTop + 28), 440.0f, 16.0f);
                DrawTextCjk(g,
                    L"3 分钟 / 赎金 5000 / 假金币 / 弹窗更多 / 随机锁桌面项 / 退出要按两次安全阀",
                    d, 11.0f, kTextFaint);
            }
```

> 中文**不能**用 Roboto Mono（`DrawTextMono` 会出豆腐块），照 `DrawTextCjk` 用（第 82~107 行有分工）。

### 4.4 交互

`OnMouse`（第 718~806 行）的 `WM_LBUTTONDOWN` 里，紧挨第 741~747 行光敏分支加：

```cpp
                if (Hit(HardHit(), p))
                {
                    g_edit.hardcore = !g_edit.hardcore;
                    PushLive();
                    // 打开开关时整扇窗抖一下，给个"这东西很重"的反馈。
                    // AnimateJolt 走真实窗口位移，动画会排队，不会和别的动画打架。
                    aero::AnimateJolt(hwnd, 9.0f, 380);
                    elog::Write(L"[setup] 硬核模式 %s", g_edit.hardcore ? L"开" : L"关");
                    aero::Repaint(hwnd);
                    return;
                }
```

`AnimateJolt` 在 `aero_window.h:157`（`void AnimateJolt(HWND, float amplitudePx, DWORD ms)`），popup.cpp:400 已在用。

滑条锁定堵三处：

1. `ApplyDrag` 第 675~677 行 `case 5:` 前加 `if (g_edit.hardcore) return;`
2. `OnMouse` 第 783~790 行金币条命中分支开头 `if (g_edit.hardcore) return;`
3. `OnWheel` 第 864~872 行金币滚轮分支开头同理

建议 `OnWheel` 第 816~822 行的复选框分支也加：**硬核开关不接受滚轮切换**（滚轮误触在这么重的开关上很烦）。

**难度 ★★☆☆☆**。**验证**：`Ransom_dev.exe --setup-ui shot.png --ui-grid`（entity_main.cpp:267）出图量像素。

---

## 5. T3 赎金 5000

见 T1 的 `GoldGoal()`。改完下游**全部自动跟着走**：

- `director.cpp:323` `gold::Spawn(settings::GoldGoal());`
- `director.cpp:361` `popup::SetStatus(g_gold, settings::GoldGoal(), ...)`
- `director.cpp:364` `if (g_gold >= settings::GoldGoal())` → 付清
- `director.cpp:569` `int GoldGoal() { return settings::GoldGoal(); }`

**难度 ★☆☆☆☆**。但**必须**和 T8 一起做，否则开局即不可通关。
（注意：T9 的假金币会在这条基础上再往上加，见 §10.4，届时这四处要改成 `EffectiveGoal()`。）

---

## 6. T4 倒计时改成 3 分钟

`kCaughtMs` 是编译期常量，被四处用：`:54` 定义、`:362`、`:401`、`:575`。

第 54 行：

```cpp
    const DWORD kCaughtMsNormal = 90000;    // 原作就是 90 秒
    const DWORD kCaughtMsHard   = 180000;   // 硬核：3 分钟
```

状态区（第 89~104 行）加：

```cpp
    // 本轮勒索的总时长与"关一个子窗口扣多少"。
    // 以前是编译期常量，硬核要按设置切换，所以进 PHASE_CAUGHT 时定下来。
    DWORD g_ransomTotalMs = kCaughtMsNormal;
    DWORD g_childCreditMs = kChildCloseCreditMs;
```

`PHASE_CAUGHT`（第 226~238 行）里，在 `g_timeCreditMs = 0;`（第 235 行）旁加：

```cpp
            g_ransomTotalMs = settings::Hardcore() ? kCaughtMsHard : kCaughtMsNormal;
            g_childCreditMs = settings::Hardcore() ? (DWORD)settings::kHardcoreCloseCreditMs
                                                  : kChildCloseCreditMs;
            elog::Write(L"[director] 本轮倒计时 %lu 秒（硬核 %s）",
                (unsigned long)(g_ransomTotalMs / 1000),
                settings::Hardcore() ? L"开" : L"关");
```

然后把第 362、401、575 行的 `kCaughtMs` 全换成 `g_ransomTotalMs`。

时间格式不用改：`ui_layout.cpp:325~328` 已是 `%02lu:%02lu`，180 秒显示 `03:00`。
`kRiserLeadMs`（第 63 行，剩 15 秒渐隐 + 叠 riser）不用改。

**难度 ★★☆☆☆**。**红线**：`g_ransomTotalMs` 必须和主题曲长度一致（T5）。

---

## 7. T5 remix 主题曲（`.ogg`）+ 3:30 → 3:00

### 7.1 素材

放到 **`assets\audio\Ransom_theme_remix.ogg`**。
`.ogg` 本来就在支持范围内（`audio_clip.cpp` 用内嵌的 stb_vorbis 解，README 第 54 行也写了"ogg/mp3 源码内联"），**格式不用改任何代码**。
放进去也不用改工程文件：构建时 `tools\gen_assets.ps1` 扫 `assets\audio\` 自动生成资源清单（脚本第 78~99 行）。

### 7.2 现状

```cpp
// audio.cpp:82-91
    const double kThemeKeepSec  = 80.0;   // 原版：保留前 80 秒
    const double kThemeOutSec   = 90.0;   // 慢放到 90 秒，正好等于 90 秒倒计时
    const double kThemeFullGain = 0.55;
```

```cpp
// audio.cpp:394-425
    void EditTheme()
    {
        audio_clip::Clip& c = g_clips[C_THEME];
        ...
        const double factor = kThemeOutSec / kThemeKeepSec;
        if (!audio_clip::StretchPitchPreserving(c, keep, factor, out)) { ... }
        c = out;
    }
```

启动时调一次：`audio.cpp:464 EditTheme();`

### 7.3 加第二轨

1. `ClipId`（第 35~46 行）加 `C_THEME_HC`，**同时**在名字表（48~58 行）和起始偏移表（67~77 行）的**同一位置**各加一项：

```cpp
        C_THEME,         // Ransom_full_theme.mp3    原版主题曲（处理成 90s）
        C_THEME_HC,      // Ransom_theme_remix.ogg   硬核主题曲（处理成 180s）
        C_GLITCHLOOP,    // GEN_GLITCH_LOOP_(...).ogg    故障底噪
```

```cpp
        L"Ransom_full_theme.mp3",
        L"Ransom_theme_remix.ogg",
        L"GEN_GLITCH_LOOP_(135402425939071).ogg",
```

```cpp
        0.0,   // C_THEME
        0.0,   // C_THEME_HC     （循环层，不走这里）
        0.0,   // C_GLITCHLOOP
```

2. `EditTheme` 参数化（第 396 行）：

```cpp
    // keepSec：只保留前多少秒；outSec：变速不变调之后的目标时长。
    //   原版：80 -> 90（慢放）；硬核 remix：210 -> 180（快进）。
    void EditTheme(ClipId id, double keepSec, double outSec, const wchar_t* tag)
    {
        audio_clip::Clip& c = g_clips[id];
        if (!c.Ok()) { elog::Write(L"[audio] %s 没载入，跳过处理", tag); return; }

        const size_t keep = (size_t)(keepSec * kSampleRate);
        if (c.Frames() <= keep)
        {
            elog::Write(L"[audio] %s %.2f 秒，不长于 %.0f 秒，跳过裁剪与变速",
                tag, c.Seconds(), keepSec);
            return;
        }

        const double factor = outSec / keepSec;
        const double before = c.Seconds();

        audio_clip::Clip out;
        const DWORD t0 = GetTickCount();
        if (!audio_clip::StretchPitchPreserving(c, keep, factor, out))
        {
            elog::Write(L"[audio] %s 变速失败，退回未处理版本（%.2f 秒）", tag, before);
            return;
        }
        const DWORD ms = GetTickCount() - t0;
        elog::Write(L"[audio] %s 已处理：原 %.2f 秒 -> 保留前 %.1f 秒 -> x%.4f -> %.2f 秒（耗时 %lums）",
            tag, before, keepSec, factor, out.Seconds(), (unsigned long)ms);
        c = out;
    }
```

第 464 行改成两次调用：

```cpp
        EditTheme(C_THEME,    80.0,  90.0, L"主题曲（原版）");
        EditTheme(C_THEME_HC, 210.0, 180.0, L"主题曲（硬核 remix）");
```

> 210 / 180 = 你要的"3:38 的曲子取前 3:30（=210s），快进不变调压到 3:00（=180s）"，factor = 0.8571。

3. `SetTheme` 按硬核选轨（第 566~573 行）：

```cpp
    void SetTheme(bool on)
    {
        // 硬核换 remix 那一轨。两轨长度分别是 90s / 180s，
        // 和各自的倒计时严格对齐，所以在这里换轨不会造成音画错位。
        g_theme.clipId = settings::Hardcore() ? C_THEME_HC : C_THEME;
        g_theme.target = on ? kThemeFullGain : 0.0;
        if (on) ++g_themeRestart;      // 从 0 秒起播
    }
```

`SeekThemeBy`（第 635~645 行）内部用的是 `g_clips[g_theme.clipId]`（第 285 行），换轨后自动跟着走，不用改。
`PreviewBgm`（第 620~623 行，设置界面试听）建议同样按硬核选轨 —— 用户在设置界面听到的就是他接下来会听到的那首。

### 7.4 ⚠ 一个真实的坑：WSOLA 搜索半径不够用

`audio_clip.cpp:283-289`：

```cpp
const int kSF    = 2048;   // 帧长
const int kSHop  = 512;    // 合成步长
const int kSSrch = 80;     // 搜索半径，要盖住 |Ha-Hs|
```

注释自己写了"要盖住 |Ha-Hs|"。分析步长 `Ha = kSHop / factor`（第 311 行）：

- 原版是**慢放**（factor 1.125）：`Ha = 455`，`|Ha-Hs| = 57 < 80` ✅ 够
- remix 是**快进**（factor 0.8571）：`Ha = 597.3`，`|Ha-Hs| = 85.3 > 80` ❌ **搜不到"自然延续点"，会挑到边界候选，听到接缝/抖动**

必须把 `kSSrch` 提到 **160**。代价是候选数 161→321、搜索开销约 2 倍；210 秒素材处理一次约 0.5~2 秒（一次性，Start() 里跑），可接受，日志里已经打了耗时。

**难度 ★★★☆☆**：代码不难，难在"听感对不对"，最后必须靠耳朵定。
**验证**：`Ransom_dev.exe --theme-dump hc.wav`，看日志 `-> 180.00 秒`，再听接缝。

---

## 8. T6 弹窗：更多 + 鼠标位阻碍 + 关窗扣 30 秒

### 8.1 常量运行时化

`popup.cpp:27-33`：

```cpp
    const DWORD kBurstLifeMs = 12000;
    const DWORD kGapMinMs = 5000;
    const DWORD kGapMaxMs = 12000;
    const DWORD kChildLifeMin = 4000;
    const DWORD kChildLifeMax = 8000;
    const int   kMaxChildren = 14;
```

`kMaxChildren` 被三处用（第 277 行上限判断、第 588 行 clamp、第 591 行 `reserve`）。改成运行时：

```cpp
    int   g_maxChildren = 14;
    DWORD g_gapMin = kGapMinMs, g_gapMax = kGapMaxMs;
```

在 `BeginRansom`（第 558 行）里按 `settings::Hardcore()` 赋值，三处 `kMaxChildren` 换成 `g_maxChildren`。
`NextWaveGap()`（148~160 行）与 `NextWaveCount()`（163~167 行）里的 `kGapMinMs/kGapMaxMs` 换成 `g_gapMin/g_gapMax`。

**"弹窗更多"最小改动清单**：

| 项 | 现在 | 硬核 |
|---|---|---|
| 开局批 `popup::BeginRansom(8)`（director.cpp:320） | 8 | 12 |
| 同时上限 `kMaxChildren` | 14 | 22 |
| 波间隔 `kGapMin/MaxMs` | 5000~12000 | 3000~7000 |
| 收尾波间隔 `kGapMin/MaxEndMs`（74~75 行） | 2500~6000 | 1500~3500 |
| 单窗存活 `kChildLifeMin/Max` | 4000~8000 | 2500~6000 |

### 8.2 在鼠标位置生成"阻碍点击"的弹窗

`SpawnChild`（第 275~326 行），落点是第 299~300 行。签名加 `bool atCursor`（第 275 行）：

```cpp
    void SpawnChild(DWORD lifeMs, bool atCursor = false)
```

位置分支：

```cpp
        if (atCursor)
        {
            // 硬核："阻碍点击" —— 直接开在光标下面，玩家要点的东西先点到它。
            // 尺寸取小的那一档，是"挡住这一下"，不是"糊满屏幕"。
            POINT m = {};
            GetCursorPos(&m);
            opt.x = m.x - w / 2;
            opt.y = m.y - h / 2;
        }
        else
        {
            opt.x = wa.left + rand() % (aw > w + 28 ? aw - w - 28 : 1);
            opt.y = wa.top + rand() % (ah > h + 28 ? ah - h - 28 : 1);
        }
        // 无论哪条路都夹回工作区，别开一半在屏幕外
        if (opt.x < wa.left) opt.x = wa.left;
        if (opt.y < wa.top)  opt.y = wa.top;
        if (opt.x + w > wa.right)  opt.x = wa.right - w;
        if (opt.y + h > wa.bottom) opt.y = wa.bottom - h;
```

尺寸：`atCursor` 时用小档（第 287~288 行改成 `w = 150 + rand()%90; h = 110 + rand()%70;`），普通批次不变。

驱动放在 `Tick()`（382~495 行）波次逻辑（467~480 行）旁：

```cpp
        // ---- 硬核：鼠标位"阻碍弹窗" ----
        if (g_hardcore && (int)g_children.size() < g_maxChildren && now >= g_nextCursorPop)
        {
            g_nextCursorPop = now + 900 + (DWORD)(rand() % 900);
            SpawnChild(1200 + (DWORD)(rand() % 1200), true);
        }
```

> 弹窗真的能挡点击：aero 窗口是真实的分层窗口，光标下的非透明像素会收到鼠标消息，所以开在光标下面就是真的挡。**不需要**动 overlay 的低层钩子。
> （反过来也说明：overlay 自己是 `WS_EX_TRANSPARENT`，它挡不了点击 —— 见 §12.3 第 8 条。）

### 8.3 关窗惩罚 30 秒

`director.cpp:68` 的 `kChildCloseCreditMs = 10000` 已在 T4 改成 `g_childCreditMs`（硬核 30000）。使用点第 347 行和第 356 行的日志文案一起换。

**难度 ★★☆☆☆**。
**风险**：硬核下 30 秒 × 6 个 = 直接超时，而倒计时是 180 秒。这是你要的强度，但请确认"关 6 个窗就必死"能接受。

---

## 9. T7 + T8 金币面额与总量（**必改**）

### 9.1 T7 面额池（Q4 = 方案 B）

硬核池在 T1 里已定为 `{ 50, 75, 100 }`。

`gold.cpp` 挑面额的是 `PickCoinAmount`（第 534~558 行），读 `settings::CoinAmounts()`（第 536 行）。两条路：

- **推荐**：让 `settings::CoinAmounts()` 自己按硬核返回不同池（settings.cpp:335），下游一行都不用改。
- 或在 `PickCoinAmount` 里 `settings::Hardcore() ? 硬核池 : settings::CoinAmounts()`。

第 539~541 行的 cap 逻辑：

```cpp
    int cap = goal * 2;
    if (cap < 10)  cap = 10;
    if (cap > 500) cap = 500;
```

硬核 goal=5000 → cap=500 → 池里 50/75/100 全部可用，**这段不用改**。

### 9.2 T8 数量重算 —— 不改就是"开局即无法通关"

现在的公式（`gold.cpp:636~650`）：

```cpp
    const int target = goal + goal * 2 / 5;      // 目标 +40% 余量
    int maxCoins = 14 + goal / 100;
    if (maxCoins > 30) maxCoins = 30;            // ← 硬核下必须放开
```

**硬核下 target = 7000，上限 30 枚 × 最高 100 = 3000，永远凑不够 5000。**

按 Q4 选的方案 B 改：

```cpp
    const bool hc = settings::Hardcore();

    // 方案 B：面额池 50/75/100（均值 75），余量降到 10%
    //   target = 5000 * 1.1 = 5500  ->  约 74 枚
    const int target = hc ? (goal + goal / 10) : (goal + goal * 2 / 5);

    int maxCoins = 14 + goal / 100;
    if (hc) maxCoins = 90;
    if (maxCoins > (hc ? 120 : 30)) maxCoins = (hc ? 120 : 30);
```

同时把"直接落在桌面根目录"的比例调高，让玩家至少能先看见一批（`gold.cpp:35`）：

```cpp
const int kDesktopRootPercent = 60;   // 25 -> 60（硬核下候选位置变多，桌面更需要露面）
```

> 为什么能放心加到 74 枚：现有清理是"清单（`%LOCALAPPDATA%\Ransom_dev\gold_manifest.txt`）+ 扫桌面找孤儿（`RemoveOrphans()` 481~516 行）"双保险，`SaveManifest` 每轮写一次（第 704 行），规模没问题。
> **但 T10 之后要注意**：`RemoveOrphans()` 只扫桌面，磁盘目录里的金币它够不着 —— 见 §11。

---

## 10. T9 假金币（Q2 已定）

### 10.1 效果映射（**注意：和你最早那句描述的方向相反，以这次为准**）

| 被污染的位置 | 例子 | 效果 | 数值 |
|---|---|---|---|
| **前缀 "Gold"** | `G01d_50` | **扣计时器** | 原本面额 × 0.1 秒（50 → 扣 5 秒；75 → 7.5 秒；100 → 10 秒） |
| **后缀数字** | `Gold_5o` | **增加未付的赎金** | 按原本面额加（50 → 目标 +50） |
| **两个都污染** | `G01d_5o` | **两种效果同时触发** | 扣 5 秒 + 目标 +50 |

所以"污染形态"是一个 **2 位掩码**，不是三选一：

```cpp
// 假金币污染掩码
const int kFakePrefix = 1;   // bit0：前缀 "Gold" 被污染 -> 扣计时器
const int kFakeSuffix = 2;   // bit1：面额数字被污染   -> 增加未付赎金
// 取值 1 / 2 / 3，3 = 两个都中
```

生成比例按 T1 的常量：前缀单污染 40%、后缀单污染 40%、双污染 20%。**这三个数你随时可以改**
（`kFakePrefixOnlyPct` / `kFakeSuffixOnlyPct` / `kFakeBothPct`）。

### 10.2 命名

同形字替换表 + **按掩码只污染对应的区段**（双污染时两段各至少换一处）：

```cpp
// ---- 假金币：同形字替换 ----
// 每一对都是"字形接近的字母 / 数字"，双向可用。
// 玩家盯着看能看出来，扫一眼看不出来 —— 这正是要的效果。
const wchar_t kFakePairs[][2] = {
    { L'o', L'0' }, { L'l', L'1' }, { L'i', L'1' }, { L's', L'5' },
    { L'g', L'6' }, { L'b', L'8' }, { L'z', L'2' }, { L'e', L'3' },
    { L't', L'7' }, { L'a', L'4' },
};
const int kFakePairCount = (int)(sizeof(kFakePairs) / sizeof(kFakePairs[0]));

// 把 s 里的一处字符换成同形字。返回是否真换掉了一处。
bool FakeSwapOne(std::wstring& s)
{
    int pos[64]; int n = 0;
    for (size_t i = 0; i < s.size() && n < 64; ++i)
        for (int k = 0; k < kFakePairCount; ++k)
            if (s[i] == kFakePairs[k][0] || s[i] == kFakePairs[k][1]) { pos[n++] = (int)i; break; }
    if (n == 0) return false;

    const size_t i = (size_t)pos[rand() % n];
    for (int k = 0; k < kFakePairCount; ++k)
    {
        if (s[i] == kFakePairs[k][0]) { s[i] = kFakePairs[k][1]; return true; }
        if (s[i] == kFakePairs[k][1]) { s[i] = kFakePairs[k][0]; return true; }
    }
    return false;
}

// 造假金币的显示名（不带扩展名）。mask 见上面的 kFakePrefix / kFakeSuffix。
// "Gold" 里可换的有 o->0、l->1、g->6，够用。
void MakeFakeStem(int amount, int mask, std::wstring& out)
{
    wchar_t digits[24];
    swprintf_s(digits, L"%d", amount);

    std::wstring pre = L"Gold", num = digits;

    // 掩码说哪段就换哪段；双污染时两段都换，保证两种效果都看得出来。
    if (mask & kFakePrefix) FakeSwapOne(pre);
    if (mask & kFakeSuffix) FakeSwapOne(num);

    // 兜底：万一某段没有可换的字符（正常不会发生，但别留隐患）
    if (pre == L"Gold" && num == digits) FakeSwapOne(pre);

    out = pre + L"_" + num;
}
```

### 10.3 记录与付款路径

`Coin` 结构（第 38~42 行）加：

```cpp
struct Coin {
    std::wstring path;
    int          amount;
    int          token;
    int          fakeMask = 0;     // 0 = 真金币；1/2/3 见 kFakePrefix / kFakeSuffix
};
```

`PickFreeName`（第 224~241 行）现在自己拼 `Gold_%d.lnk`，改成收"名字主体"：

```cpp
bool PickFreeName(const std::wstring& dir, const std::wstring& stem, std::wstring& outPath)
{
    for (int suffix = 0; suffix < 40; ++suffix)
    {
        wchar_t name[128];
        if (suffix == 0) swprintf_s(name, L"%s.lnk", stem.c_str());
        else             swprintf_s(name, L"%s_%d.lnk", stem.c_str(), suffix);
        ...
    }
}
```

> 小提醒：假币撞名时加 `_1` 后缀会让"看名字辨真假"变模糊。建议**假币不加后缀**，撞名就跳过这一次生成（`continue`），保证假币名字永远是"干净的同形替换"。

`Spawn`（第 620~720 行）里，第 665~672 行附近插：

```cpp
        const int amount = PickCoinAmount(goal);
        const int token  = g_nextTok++;

        // 假金币：硬核下按比例生成，mask 决定污染哪一段（可叠加）
        int fakeMask = 0;
        if (settings::Hardcore() && (rand() % 100) < settings::kHardcoreFakePercent)
        {
            const int r = rand() % 100;
            if (r < settings::kFakePrefixOnlyPct)                        fakeMask = kFakePrefix;
            else if (r < settings::kFakePrefixOnlyPct +
                         settings::kFakeSuffixOnlyPct)                   fakeMask = kFakeSuffix;
            else                                                         fakeMask = kFakePrefix | kFakeSuffix;
        }

        std::wstring stem;
        if (fakeMask) MakeFakeStem(amount, fakeMask, stem);
        else          { wchar_t b[32]; swprintf_s(b, L"Gold_%d", amount); stem = b; }

        std::wstring lnk;
        if (!PickFreeName(dir, stem, lnk)) continue;   // 假币撞名直接跳过，不加数字后缀
```

并把 `c.fakeMask = fakeMask;` 写进第 692~696 行的登记块。

`gold.h:43` 的 `Consume` 加回传参数（带默认值，老调用点不改也能编）：

```cpp
// 按 token 消费一枚金币（双击付款到达时调用）。
// 找到则删除该文件并返回金额；找不到返回 0。
// fakeMask 非空时回填污染掩码：0 = 真金币，1 = 前缀，2 = 后缀，3 = 两个都中。
int Consume(int token, int* fakeMask = nullptr);
```

`Consume`（第 722~741 行）在 `const int amount = g_coins[i].amount;` 后加：

```cpp
        if (fakeMask) *fakeMask = g_coins[i].fakeMask;
```

### 10.4 接进 director / entity_main

`entity_main.cpp:114~130` 改成：

```cpp
        int fakeMask = 0;
        const int got = gold::Consume(token, &fakeMask);
        if (got > 0)
        {
            if (fakeMask == 0)
            {
                director::CreditGold(got);            // 真金币：正常付款
                audio::PlayCoin();
            }
            else
            {
                // 假金币**不能付款**：一分钱都不进账，只按污染部位罚。
                // 两种惩罚可以同时生效（mask = 3）。
                if (fakeMask & 1) director::PenalizeTime((DWORD)got * 100);   // 面额 × 0.1 秒
                if (fakeMask & 2) director::PenalizeGoal(got);                // 未付赎金 += 面额
                audio::PlayError();
            }
        }
```

`director.h` 新增：

```cpp
// 假金币的两种惩罚。
void PenalizeTime(DWORD ms);   // 倒计时往前扣
void PenalizeGoal(int amount); // 未付的赎金 += amount（把目标顶上去）
```

`director.cpp` 实现（放第 558~566 行的 `CreditGold` 旁边）：

```cpp
    void PenalizeTime(DWORD ms)
    {
        if (g_phase != PHASE_CAUGHT) return;
        g_timeCreditMs += ms;          // 复用"关窗口提前量"那套：倒计时/音乐一起走
        audio::SeekThemeBy((double)ms / 1000.0);
        elog::Write(L"[director] 假金币（前缀污染）：倒计时扣 %.1f 秒", ms / 1000.0);
    }

    void PenalizeGoal(int amount)
    {
        if (g_phase != PHASE_CAUGHT) return;
        g_goalExtra += amount;         // 见下面"会回落"的机制
        elog::Write(L"[director] 假金币（后缀污染）：赎金 +%d，现在要付 %d",
            amount, EffectiveGoal());
    }
```

#### 10.4.1 R2："会回落" 的机制（**rev3 新增，重要**）

你说假金币顶上去的赎金**会回落**，那它就不是"永久抬高门槛"，而是**一笔要先还清的债**。
规则定为：

> **真金币进账时，先拿去填假金币留下的那笔债；填平之后，剩下的才算进付款进度。**

对应 `CreditGold`（`director.cpp:558-566`）要改成：

```cpp
    void CreditGold(int amount)
    {
        if (g_phase != PHASE_CAUGHT) return;
        if (amount <= 0) return;

        // 假金币顶上去的那部分先还。还债的这段时间里你一分钱都没往目标上挪，
        // 但债一还完，"还差多少"就回到正常下降的节奏 —— 这就是"会回落"。
        if (g_goalExtra > 0)
        {
            const int pay = (amount < g_goalExtra) ? amount : g_goalExtra;
            g_goalExtra -= pay;
            amount      -= pay;
            elog::Write(L"[director] 真金币 %d 先还假币的债，债还剩 %d", pay, g_goalExtra);
        }

        if (amount <= 0) { elog::Write(L"[director] 这笔金币全部用来还债了"); return; }

        g_gold += amount;
        elog::Write(L"[director] 收到 %d Gold（累计 %d / %d）",
            amount, g_gold, EffectiveGoal());
    }
```

为什么这么写（两个容易踩的点）：

1. **不能既减 `g_goalExtra` 又把整笔加进 `g_gold`**。`{left}` 显示的是 `目标 - 已付 = 基准 + extra - gold`，
   如果还债的 50 同时又进了 `g_gold`，读数会一次掉 100，玩家会以为见鬼了。
   上面这段是"先抵债、抵完的余量才进 gold"，`left` 全程单调下降，不会跳。
2. **债只被真金币抵，不被时间抵**，也不会因为捡到另一枚假币而"冲抵"（假币只会让债更大）。

> 如果你的"回落"指的其实是**随时间自然衰减**（比如每 10 秒自己掉一点），那是另一个实现：
> 在 `Tick()`（director.cpp:284-440）的 CAUGHT 分支里按 `kChildCloseCreditMs` 那套节奏减 `g_goalExtra` 就行。
> 我按"被真金币抵掉"写的 —— 这是最贴合"捡金币"这个核心循环的解释，**如果理解错了告诉我，改一个函数的事**。

#### 10.4.2 目标变大的连锁改动

现在赎金目标散在四处读 `settings::GoldGoal()`，加了 `g_goalExtra` 之后必须统一成一个函数，否则"显示的目标"和"判定的目标"会打架：

```cpp
    // 赎金目标 = 设置里的基准值 + 假金币顶上去的那部分。
    // 所有判定和显示都必须走这一个函数（director.cpp:361 / 364 / 565 / 569 四处）。
    int g_goalExtra = 0;

    int EffectiveGoal() { return settings::GoldGoal() + g_goalExtra; }
```

- 第 361 行：`popup::SetStatus(g_gold, EffectiveGoal(), director::RansomRemainMs(), g_ransomTotalMs);`
- 第 364 行：`if (g_gold >= EffectiveGoal())`
- 第 565 行日志、第 569 行 `GoldGoal()` 返回值同样换成 `EffectiveGoal()`
- 第 230 行 `PHASE_CAUGHT` 里 `g_gold = 0;` 旁边补 `g_goalExtra = 0;`（每轮清干净）

主窗口上 `{goal}` / `{left}` 是 `ui_layout` 现成的标记（`assets\main_window.ini:29-31`），目标一变大，玩家会**当场看到"还差多少"往上跳**；之后捡真金币还债，读数再掉回正常节奏 —— 这就是"能看见的假币"和"能看见的还债"，不需要额外做特效。

**难度 ★★★☆☆**。跨进程协议不用改：付费子进程仍是 `--pay <金额> --token <编号>`（`entity_main.cpp:258~259` 解析，`gold.cpp:672` 生成），真假信息全在主进程的 `g_coins` 里，靠 `Consume` 回传。

---

## 11. T10 金币生成到磁盘顶层目录（Q3 已定）

范围：**所有固定磁盘的顶层目录 + 原本的桌面范围**（当前用户桌面顶层文件夹 + 公共桌面顶层文件夹 + 两个桌面根目录）。

现在只扫桌面顶层文件夹：`gold.cpp:143~172`。

新增：

```cpp
// 硬核：候选位置扩到各固定磁盘的**顶层**目录（深度 1，不递归）。
// 明确排除系统目录 —— 这是整蛊程序，不该往 C:\Windows 这类地方写东西。
bool IsSystemDirName(const std::wstring& name)
{
    static const wchar_t* kBad[] = {
        L"windows", L"program files", L"program files (x86)", L"programdata",
        L"$recycle.bin", L"system volume information", L"perflogs",
        L"recovery", L"msocache", L"$windows.~bt", L"$windows.~ws",
        L"documents and settings",
    };
    const std::wstring low = ToLowerName(name);   // 自己写个小写化，或复用现成的
    for (int i = 0; i < _countof(kBad); ++i) if (low == kBad[i]) return true;
    return false;
}

void ScanFixedDriveRoots(std::vector<std::wstring>& out, int limit)
{
    const DWORD mask = GetLogicalDrives();
    for (int d = 0; d < 26 && (int)out.size() < limit; ++d)
    {
        if (!(mask & (1u << d))) continue;

        wchar_t root[8] = { (wchar_t)(L'A' + d), L':', L'\\', 0 };
        if (GetDriveTypeW(root) != DRIVE_FIXED) continue;   // 只碰固定盘，U 盘/光驱不动

        const std::wstring pattern = std::wstring(root) + L"*";
        WIN32_FIND_DATAW fd;
        HANDLE h = FindFirstFileW(pattern.c_str(), &fd);
        if (h == INVALID_HANDLE_VALUE) continue;
        do {
            if (!(fd.dwFileAttributes & FILE_ATTRIBUTE_DIRECTORY)) continue;
            if (wcscmp(fd.cFileName, L".") == 0 || wcscmp(fd.cFileName, L"..") == 0) continue;
            if (IsSkippableDir(fd.dwFileAttributes)) continue;      // 复用第 134 行那个
            if (IsSystemDirName(fd.cFileName)) continue;
            out.push_back(std::wstring(root) + fd.cFileName);
        } while (FindNextFileW(h, &fd));
        FindClose(h);
    }
}
```

`CollectTargets`（第 167 行）：

```cpp
void CollectTargets(std::vector<std::wstring>& out)
{
    out.clear();
    ScanOneDesktop(CSIDL_DESKTOPDIRECTORY,        out);
    ScanOneDesktop(CSIDL_COMMON_DESKTOPDIRECTORY, out);

    // 硬核：加上固定盘顶层目录。上限 200 个，免得在装满项目的盘上扫太久。
    if (settings::Hardcore()) ScanFixedDriveRoots(out, 200);
}
```

**写失败**已有兜底（`Spawn` 第 683~707 行的 `failed` 计数 + "另有 N 次写入失败（多为权限不足）"），不用额外加。

**⚠ 清理必须一起改（不改会漏）**：`RemoveOrphans()`（第 481~516 行）只扫桌面，磁盘目录里的孤儿金币它够不着。强杀之后就只能靠清单（`gold_manifest.txt`），而清单本身也可能没写全。建议：

```cpp
// RemoveOrphans 里把候选目录从"桌面"换成"CollectTargets() 的结果"。
// 它的判据本来就是"目标是本程序 + 参数含 --pay/--token"（IsOurCoin，466~476 行），
// 扩展到磁盘顶层同样安全 —— 不会误删任何不是我们生成的东西。
```

**风险**：杀软对"往各磁盘目录写 .lnk"比写桌面敏感得多。自己玩没问题；要发布建议留个开关。

**难度 ★★☆☆☆**。

---

## 12. T11 随机锁定非快捷方式 0.9~9 秒 → 变绿淡出（Q5：最多 9 个）

这是全案最重的一块，它要动 `desktop_overlay.cpp` 渲染循环的核心假设。

### 12.1 现在的实现事实（先看清楚再改）

| 事实 | 位置 |
|---|---|
| 只处理 `.lnk` / `.url`，扩展名判定 | `desktop_overlay.cpp:176-178`（`IsShortcutFileName`） |
| 候选名单来自文件系统扫桌面，**只在 Start() 建一次** | `:199-210` 扫盘、`:1880-1888` 建表 |
| 列表项过滤点："不在名单里就跳过" | `:420-423`（ListView 路径）、`:603-605`（COM 兜底路径） |
| 每项数据只有 `Frame{ name, rc }`，**出循环就丢名字** | `:345-348`、`RefreshInternal:1703-1794` |
| 跨帧保留的只有两个平行 RECT 数组 | `g_lastRects` / `g_hitRects`，`:1325-1333` |
| **"是否锁定"是全帧唯一一个 bool** | `:796-798` |
| 变绿 + 淡出只在"付清消散"里，按全局进度 `paidP[i]` | `:819-847`、`:863-889`、`:911-920`、`:943-952` |
| 命中判定用 `g_hitRects`，与外观无关 | `PointInLockedFrame:1415-1425` |
| 清理点：非锁定态清空、消散结束复位 | `:778-782`、`:1811-1829` |

> ⚠ **既存行为，会被 T11 踩到**：金币 `.lnk` 是在 `overlay::Start()` **之后**才生成的，
> 而 overlay 的快捷方式名单只在 Start() 建一次（`:1880-1888`），所以**金币不在名单里**。
> 硬核要"锁非快捷方式"时如果直接取"不在名单里的项"，**金币会被当成非快捷方式一起锁上**。
> 必须显式排除金币：真币 `Gold_*`，假币是同形替换后的变体（`G01d_*` / `8old_*` / `Gold_5o` …）。
> **最稳的判法**不是匹配名字，而是用 `gold::IsOurCoinFile(path)`（`gold.h:59`，判据"目标是本程序 + 参数含 --pay/--token"）
> —— 它对真假币一视同仁，也不会误伤用户自己的东西。

### 12.2 改造方案（最小改动路径，4 步）

**第 1 步：把名字保留到跨帧状态**

状态区（`:1320-1333`）加：

```cpp
std::vector<std::wstring> g_hitNames;   // 与 g_hitRects 同索引
```

在 `RefreshInternal` 填 `g_hitRects`（`:1725-1735`）的同一个循环里 `push_back(fresh[i].name)`。

**第 2 步：拿到"非快捷方式"的框**

`BuildFramesViaListView`（`:391-490`）现在第 423 行直接 `continue` 掉非快捷方式。改成收进第二个数组：

```cpp
static bool BuildFramesViaListView(const DesktopRef& d,
                                   const std::set<std::wstring>& shortcutNames,
                                   std::vector<Frame>& frames,
                                   std::vector<Frame>& others)   // ← 新增
...
        if (shortcutNames.find(ToLower(text)) == shortcutNames.end()) {
            // 不在名单里 = 文件夹 / 文件 / 虚拟项（此电脑、回收站）/ 我们自己的金币。
            // 硬核要拿它们做"随机锁定"，所以这里把框也算出来收进 others，
            // 后面统一过滤掉金币和虚拟项（见第 3 步）。
            if (g_extraLockOn) { /* 用同一段几何计算，push 进 others */ }
            continue;
        }
```

几何计算（`:437-484`）建议提成 `static RECT FrameRectOf(ListViewReader&, int i, ...)`，两条路复用，别复制 50 行。

**第 3 步：per-item 计时（照抄现成的键设计）**

`g_frameNextBurst`（`:380`）用 `(left, top)` 当身份键，注释解释了为什么不用下标（`:372-379`）。照抄：

```cpp
// 硬核：额外被锁的非快捷方式项 -> 解锁时刻 / 开始淡出的时刻 / 释放后的冷却。
// 键和 g_frameNextBurst 一样用 (left, top)：桌面重排时不能拿下标当身份。
std::map<std::pair<LONG, LONG>, DWORD> g_extraUnlockAt;
std::map<std::pair<LONG, LONG>, DWORD> g_extraFadeAt;
std::map<std::pair<LONG, LONG>, DWORD> g_extraCoolAt;

const DWORD kExtraFadeMs = 600;   // 释放时的"变绿 + 淡出"时长（和付清消散同一套观感）
```

在 `RefreshInternal` 里处理 `others`：

```cpp
for (每个 others 里的框 f) {
    const std::pair<LONG,LONG> key(f.rc.left, f.rc.top);

    // 冷却中：什么都不做（既不复锁，也不画框）
    auto cool = g_extraCoolAt.find(key);
    if (cool != g_extraCoolAt.end()) {
        if (now < cool->second) continue;
        g_extraCoolAt.erase(cool);
    }

    auto it = g_extraUnlockAt.find(key);
    if (it == g_extraUnlockAt.end()) {
        // 新名额：最多 kExtraLockMax(9) 个。map 的键天然保证"同一项不会被锁两次"。
        if ((int)g_extraUnlockAt.size() >= settings::kExtraLockMax) continue;
        g_extraUnlockAt[key] = now + settings::kExtraLockMinMs +
            (DWORD)(rand() % (settings::kExtraLockMaxMs - settings::kExtraLockMinMs + 1));
        // -> 进 rects，标记"锁定中"
    } else if (now < it->second) {
        // 还在锁：进 rects
    } else {
        // 到点：进入变绿淡出窗口
        if (g_extraFadeAt.find(key) == g_extraFadeAt.end()) g_extraFadeAt[key] = now;
        if (now - g_extraFadeAt[key] < kExtraFadeMs) { /* 进 rects，标记"正在淡出" */ }
        else {
            // 彻底放掉：进冷却，冷却完才可能被重新选中
            g_extraUnlockAt.erase(key);
            g_extraFadeAt.erase(key);
            g_extraCoolAt[key] = now + settings::kExtraLockCooldownMs;
        }
    }
}
```

**"不允许同时锁定一个"** 由三件事保证：
1. 键是 `(left,top)` 的 map —— 同一个位置不可能有两条记录；
2. 释放后进冷却（9 秒，`kExtraLockCooldownMs`），这期间不会被重新选中；
3. 选名额时只在"当前没有任何记录的项"里挑。

**第 4 步：渲染与判定同步（关键）**

`Render`（`:763`）只收 `std::vector<RECT>`。不想改签名（改了要动 4 个调用点 `:1717`、`:1792`、`:1837`、`:2091`），就用**同索引的全局标记数组**：

```cpp
// 与 rects 同索引：0=普通锁定，1=正在变绿淡出（进度另算）
std::vector<BYTE>  g_rectExtraFade;
std::vector<DWORD> g_rectFadeElapsed;
```

改渲染循环（`:796-798` 与 `:863-889`）：

```cpp
                // 原来：const bool locked = (g_opt.look == LOOK_LOCKED) || paidActive;
                // 现在逐项判定：额外锁的项有自己的生命周期
                const bool extraFading = (i < g_rectExtraFade.size() && g_rectExtraFade[i]);
                ...
                if (extraFading) {
                    // 和付清消散同一套：先变绿，再整体淡出
                    const float p = (float)g_rectFadeElapsed[i] / (float)kExtraFadeMs;
                    greenMix = (p < 0.40f) ? (p / 0.40f) : 1.0f;   // ← 这里要真渐变
                    stopAlpha = 1.0f - (p < 0.55f ? p / 0.55f : 1.0f);
                    if (p > 0.45f) boxAlpha = 1.0f - (p - 0.45f) / 0.55f;
                }
                else if (paidActive) { /* 原逻辑不动 */ }
```

> ⚠ **澄清**：现在代码里 `greenMix` 是**直接赋 `1.0f`**（`:875`），也就是"啪一下跳纯绿"，不是红→绿插值
> （注释 `:871-874` 是这么写的；`desktop_overlay.h:79` 和 `director.cpp:244` 说的"红→绿渐变"与代码不符）。
> 你要的是"**变绿渐变**"，所以这次顺手把它改成按进度插值（`:914-918` 和 `:946-948` 的插值公式本来就在，只是 greenMix 恒为 1）。
> 这段同时被"付清消散"用；只想改硬核逐项解锁、不想动付清表现的话，就把 `greenMix` 按 `extraFading / paidActive` 分开算。

**判定会自动同步 —— 这是这个设计最大的好处**：彻底放掉的项这一轮**不会**被 append 进 `g_hitRects`，所以 `PointInLockedFrame`（`:1415-1425`）自然不再拦它。**不要**把已放掉的项留在 `g_hitRects` 里。

**"按原本锁定快捷方式那样阻止交互"** 也不用额外写：`BlockingNow()`（`:1427-1430`）是全局的（`g_blockContextMenu && look == LOOK_LOCKED`），只要额外锁的框进了 `g_hitRects`，右键 / 拖动 / 吞左键三条就自动生效。

清理点两处必须一起加：
- `:778-782`（非锁定态清空 `g_frameNextBurst` 处）→ 一并清 `g_extraUnlockAt / g_extraFadeAt / g_extraCoolAt`
- `:1811-1829`（消散结束复位处）→ 同上

**新增对外 API**（`desktop_overlay.h`，放第 126 行 `Refresh` 附近）：

```cpp
// 硬核模式：除了快捷方式，再随机挑几个桌面上的**非快捷方式**项
// （文件夹 / 文档；虚拟项与金币由内部排除），锁上 0.9~9 秒，
// 同时最多 kExtraLockMax 个，同一个项不会被重复锁定。
// 到点后按"付清消散"那套变绿 + 淡出单独解锁。
// 只在 LOOK_LOCKED 期间有意义，退出锁定态会自动全部清掉。
void SetExtraLockEnabled(bool on);
bool ExtraLockEnabled();
```

`director.cpp` 在进锁定态（第 320~322 行）时 `overlay::SetExtraLockEnabled(settings::Hardcore());`，在 `PHASE_IDLE`/`ESCAPED`/`PAID`/`PUNISH` 的复位处（第 158~159、221~222 行）关掉。

### 12.3 这块最容易踩的坑（都有代码依据）

1. **`locked` 是全帧单值**（`:797`），不拆成逐项判定，"只解锁某一个"根本做不到。
2. **判定用 `g_hitRects`、绘制用 `g_lastRects`**（`:1327-1328`）：只改外观不改判定 → "框变绿了但点击还是被吞"。本方案靠"放掉的项不进 `g_hitRects`"天然对齐，别破坏它。
3. **虚拟项要排除**："此电脑"这类在 COM 路径会返回 `::{GUID}`（`:603-604` 注释），ListView 路径下是本地化字符串。**别比中文串**，用"解析路径为空 / 以 `::` 开头"来判。
4. **金币要排除**，而且要用 `gold::IsOurCoinFile()` 判（见 12.1 的既存行为）。
5. **别漏故障粒子**：粒子生成条件是"落在某个锁定框里"（`:1008` 附近），`alive` 集合在 `:1030` 收集。已释放的项如果不同时从这两处排除，会出现"框已经没了、粒子还在原地冒"。
6. `RefreshInternal` 在 `!changed` 时提前返回（`:1777-1781`），但 `g_hitRects` 在返回**之前**已经更新（`:1725-1735`）—— 这是刻意的，别挪。
7. `OverlayTick` 消散结束时**直接**写 `g_opt.look` 并 `ShowWindow(SW_HIDE)`（`:1820-1825`），绕过 `SetLook`/`SetVisible`；新增状态记得在那里一起复位。
8. 覆盖层窗口带 `WS_EX_TRANSPARENT`（`:672`），**自己收不到鼠标消息**，所以"挡住点击"这件事**不能**靠 overlay 做 —— T6 的鼠标位弹窗走的是真实 aero 窗口，那才是正路。

**难度 ★★★★☆**，本次最花时间的一项。建议**单独开一轮做**，做完先用 `--no-overlay` 对照，再开 overlay 慢慢看。

---

## 13. T13 硬核下安全阀要按两次（Q6）

> **这是本次唯一一处安全相关改动，请重点看。** 安全阀 `Ctrl + Alt + Shift + Q`（`entity_main.cpp:65-67`）
> 是"任何时候想立刻停"的最后一根绳子。改成按两次，等于人为加了一道延迟 —— 必须设计得足够明确，否则就是红线破了。

### 13.1 一个必须坚持的前提

**第一次按下必须有清晰、不可忽视的反馈**，否则用户会以为程序没响应，然后开始砸键盘 / 拔电源。
建议反馈组合拳（我推荐全给）：

1. `audio::PlayError()` —— 立刻"哔"一声（`Ransom_UI_-_Error.ogg`，已经在用）；
2. 弹一个 aero 小提示窗：`再按一次 Ctrl + Alt + Shift + Q 立刻退出`（**存活时长跟武装窗口一致，9 秒**——提示比窗口先消失的话，用户第二次按下时屏幕上什么都没了）；
3. 写日志（`elog::Write`，`--diag` 能捞到）。

方案 2 最省事的实现：`aero_window` 本来就是"自绘 + 逐像素透明 + 支持任意尺寸"，照 `setup_ui.cpp` 的 `notice` 命名空间（第 934 行起）抄一个 340×110 的小窗，一屏字，定时器到点自己 `aero::AnimateClose`。约 60~80 行。

### 13.2 落点

`entity_main.cpp:686-695` 是主消息循环里的热键处理：

```cpp
        if (msg.message == WM_HOTKEY)
        {
            if (msg.wParam == kHotkeyPanic)
            {
                elog::Write(L"安全阀触发，退出");
                g_running = false;
                break;
            }
            continue;
        }
```

改成：

```cpp
        if (msg.message == WM_HOTKEY)
        {
            if (msg.wParam == kHotkeyPanic)
            {
                // 硬核：第一次只"武装"，第二次才真退。
                // 普通模式 / 演出开始前的两屏：一次即退（那时候什么都还没动）。
                if (PanicConfirm()) { elog::Write(L"安全阀触发，退出"); g_running = false; break; }
                continue;
            }
            continue;
        }
```

状态与判定（放 `entity_main.cpp` 匿名 namespace，第 72 行 `g_running` 旁边）：

```cpp
// 安全阀"按两次"的武装状态。
//   g_panicArmAt : 第一次按下的时刻；超过窗口自动解除，
//                  避免"很早以前按过一次"然后某次误触直接退出
DWORD g_panicArmAt = 0;
const DWORD kPanicArmWindowMs = 9000;   // 两次之间 9 秒内有效（R3 已定）

bool PanicConfirm()
{
    // 非硬核：一次即退，行为和 0.3 完全一样
    if (!settings::Hardcore()) return true;

    const DWORD now = GetTickCount();
    if (g_panicArmAt != 0 && now - g_panicArmAt <= kPanicArmWindowMs)
    {
        g_panicArmAt = 0;
        return true;                       // 第二次：真退
    }

    g_panicArmAt = now;
    audio::PlayError();                    // 反馈 1：一声
    elog::Write(L"[panic] 硬核模式：安全阀已武装，%.1f 秒内再按一次即退出",
        kPanicArmWindowMs / 1000.0);
    setup_ui::ShowPanicConfirm(kPanicArmWindowMs);   // 反馈 2：屏幕上的提示窗
    return false;
}
```

`MOD_NOREPEAT` 已经在（`:66`），所以**按住不放不会自动重复触发**，两次必须是货真价实的两下 —— 这点不用额外处理。

### 13.3 演出开始前的那两屏

设置窗口 / 应急提示窗口自带模态循环，热键走的是另一条路：`entity_main.cpp:102-105` → `setup_ui::DispatchHotkey()` → `HotkeyWhileSetup`（`setup_ui.cpp:174-178`）。

**建议这两屏保持"一次即退"**：那时候金币还没生成、窗口还没被收走、桌面还没被动过，没有任何需要"防止手滑"的东西，单次退出反而更安全。
如果你要"硬核下任何时刻都两次"，那就把 `PanicConfirm()` 也搬进 `HotkeyWhileSetup` —— 一行的事，但我不推荐。

### 13.4 文案要跟着改（否则就是"骗人"）

- `setup_ui.cpp:634-637` 设置窗口底部提示：`开始后随时可以按 Ctrl + Alt + Shift + Q 立刻停下并全部还原。`
  → 硬核打开时改成：`硬核模式：开始后要连按两次 Ctrl + Alt + Shift + Q 才会停下。`
- `setup_ui.cpp:1012-1019` 应急提示窗口那个高亮框：`想立刻停：按 Ctrl + Alt + Shift + Q` + `这是全局热键，任何时候都有效，按下去立刻停下并全部还原。`
  → 硬核下补一句"（硬核模式需要连按两次）"。
- `README.md:117` 的安全阀表格一行。
- `setup_ui.cpp:1175-1178` 的关闭按钮路径不受影响（点 X 依然是"中止不演出"）。

**难度 ★★★☆☆**（代码不多，但要保证反馈一定看得见）。

---

## 14. T12 接线与收尾

- `director.cpp:320` `popup::BeginRansom(8)` → 硬核 12。
- `director.cpp:323` `gold::Spawn(settings::GoldGoal())` 不变（T3 覆盖）。
- `director.cpp:334` `audio::SetTheme(true)` 不变（T5 按硬核选轨）。
- `director.cpp:363` 那条"进入勒索阶段"的日志补上硬核标记和本轮参数（时长 / 目标 / 假币比例），排查全靠日志。
- `entity_main.cpp:390` 附近的 `settings::Set demo;`（`--setup-ui` 预览样例）建议加一组硬核样例，把灰化、勾选态也导出来看。
- `README.md`：第 8 行开头的机制说明、"演出流程"（128~150 行）、"命令行开关"表、安全阀表格都要补硬核说明。
- `settings.cpp:137-170` 的 ini 注释块补 `hardcore=`。

**难度 ★★☆☆☆**。

---

## 15. 已确认决定汇总（存档，防止日后走样）

| 项 | 决定 |
|---|---|
| 硬核入口 | 启动设置界面加一个复选框，打开时窗口抖一下 |
| 赎金滑块 | 硬核下变灰锁定，显示 5000 |
| 倒计时 | 硬核 3 分钟（180 秒），主题曲 180 秒严格对齐 |
| 主题曲 | 换 `Ransom_theme_remix.ogg`，取前 3:30 快进不变调压到 3:00 |
| 赎金 | 5000 |
| 金币面额 | 硬核池 `{50, 75, 100}`（方案 B），约 74 枚 |
| 假金币 | 前缀污染 → 扣计时器(面额×0.1 秒)；后缀污染 → 未付赎金 += 面额；**可叠加** |
| 假金币不能付款 | 一分钱不进账 |
| 假金币的债 | **会回落**：真金币先拿去抵债，抵平后才算进付款进度 |
| 假币形态比例 | 前缀 40% / 后缀 40% / 两个都中 20% |
| 金币范围 | 桌面（原范围）+ 所有固定磁盘顶层目录（扫描上限 200 个目录） |
| 弹窗 | 更多（上限 22）、在鼠标位置生成阻碍点击 |
| 关窗惩罚 | 硬核 30 秒/个（关 6 个即超时，接受） |
| 非快捷方式锁定 | 0.9~9 秒，同时最多 **9** 个，同一项不重复锁，释放后冷却 **9 秒** |
| 安全阀 | 硬核下**连按两次**才退出（窗口 **9 秒**），第一次必须有可见反馈 |
| 演出开始前的两屏 | 仍是**一次即退**（那时候还没动任何东西） |
| 限时金币 | **本轮取消** |
| 中途切换 | **不允许**，本轮按开始时的设置跑到底 |

### R1~R7 确认结果（rev3，**已无待定项**）

| # | 问题 | 你的决定 | 落到代码里 |
|---|---|---|---|
| R1 | 假金币三种形态的比例 | 默认 40% / 40% / 20% | `kFakePrefixOnlyPct` / `kFakeSuffixOnlyPct` / `kFakeBothPct` |
| R2 | 假币顶上去的赎金**会不会回落** | **会回落** | `CreditGold()` 先抵 `g_goalExtra` 再加 `g_gold`，见 §10.4.1 |
| R3 | 两次安全阀之间的有效窗口 | **9 秒** | `kPanicArmWindowMs = 9000`，提示窗存活同步 9 秒 |
| R4 | 演出开始前那两屏是否也按两次 | **不用**（保持一次即退） | 只有主消息循环那条路走 `PanicConfirm()` |
| R5 | 磁盘顶层目录扫描上限 | 默认 200 | `ScanFixedDriveRoots(out, 200)` |
| R6 | 锁定项释放后多久可再被选中 | **9 秒** | `kExtraLockCooldownMs = 9000` |
| R7 | 硬核下"关 6 个子窗口 = 直接超时" | 默认（接受） | 30 秒 × 6 = 180 秒 |

---

## 16. 验证清单（每步做完对着跑）

| 场景 | 命令 / 操作 | 看什么 |
|---|---|---|
| 设置界面排版（硬核行 + 灰化） | `Ransom_dev.exe --setup-ui setup.png --ui-grid` | 网格数像素，硬核行有没有压到按钮 |
| 硬核主题曲处理 | `Ransom_dev.exe --theme-dump hc.wav` | 日志 `-> 180.00 秒`；**听接缝**（WSOLA 搜索半径那个坑） |
| 倒计时显示 | `Ransom_dev.exe --phase 被抓 --no-auto --diag run.log` | 窗口显示 `03:00`；日志"本轮倒计时 180 秒" |
| 金币规模 | 同上 + 看 `run.log` | 生成枚数（约 74）、总额、假币比例 |
| 假金币（后缀） | 双击一个 `Gold_5o` 样式的金币 | `{left}` 读数往上跳（目标变大） |
| 假金币（前缀） | 双击一个 `G01d_50` 样式的金币 | TIME 直接少 5 秒 |
| 假金币（叠加） | 双击 `G01d_5o` | 时间少 5 秒 **且** 目标 +50 |
| **假币的债会回落** | 先点一枚 `Gold_5o`（+50），再点一枚真的 `Gold_50` | 读数先涨 50，再掉回原来那一条线上；日志出现"先还假币的债" |
| 磁盘目录生成 | 跑完看 `run.log` 与 `C:\` `D:\` 顶层 | 金币确实落在固定盘顶层，且没碰系统目录 |
| 清理是否干净 | `Ransom_dev.exe --clean-gold` | 桌面**和磁盘目录**的金币全部清掉 |
| 桌面逐项锁定 | 全程跑一轮 + `--diag run.log` | 非快捷方式被锁 0.9~9 秒；同时不超过 9 个；到点变绿淡出；**淡出后点击不再被吞** |
| 同一项不重复锁 | 长时间观察同一个文件夹 | 不会出现"刚变绿又被锁"（释放后有 9 秒冷却） |
| 安全阀（普通） | 演出中按一次 `Ctrl+Alt+Shift+Q` | 立刻退出并全还原 |
| 安全阀（硬核） | 演出中按一次 | **必须**有提示（音效 + 屏幕文字）；再按一次才退出 |
| 安全阀（超时） | 按一次后等 10 秒再按一次 | 不退出，重新武装（窗口是 9 秒） |
| 开演前两屏 | 在设置/提示窗口按一次 | 一次即退（R4：不走两次逻辑） |
| 照旧回归 | `--clean-gold`、`--restore`、`--no-lockdown`、`--no-overlay`、`--no-block-menu` | 全部照旧可用（硬核不许把这些开关弄失效） |

日志开关是 `--diag PATH`（README 第 167 行），GUI 子系统没有控制台，排查全靠它。

---

## 17. 红线（硬核也不许破）

1. **只写 `.lnk`，绝不写可执行文件**（gold.h 第 11 行那条安全边界）。
2. **绝不覆盖已存在的文件**（`PickFreeName` 第 226~240 行）。
3. **只删登记过的路径**，不做通配删除（`Cleanup` 第 743~756 行）；磁盘目录扩展之后，`RemoveOrphans()` 仍必须靠"目标是本程序 + 参数含 --pay/--token"判定，不许放宽成按文件名匹配。
4. **退出路径必须全还原**：付清 / 超时 / 安全阀 / 关窗口四条路都要走 `lockdown::Restore()` + `gold::Cleanup()`（director.cpp:241、252~255、266~268、508~509）。
5. **安全阀任何时候有效**：硬核下改成两次，但**第一次必须给出可见反馈**（音效 + 屏幕提示），否则视为红线破了。
6. 硬核**不许**把 `--no-lockdown` / `--no-overlay` / `--no-block-menu` / `--clean-gold` / `--restore` 这些排查开关弄失效。
7. **不在系统目录写任何东西**（`IsSystemDirName` 那张排除表是底线）。

---

*方案 rev3 完。**所有需求与参数均已确认，无待定项。** 说一声"开工"即可从 T1 开始。*
